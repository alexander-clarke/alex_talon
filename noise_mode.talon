noise (on | enable):
  mode.enable("noise")

noise (off | disable):
  mode.disable("noise")

speech noise (on | enable):
  mode.enable("user.speech_noise")

speech noise (off | disable):
  mode.disable("user.speech_noise")
